# Optional local-only overrides.
# Copy this file to `config/model-overrides.local.env` and edit the values you
# want to change on your machine. The `.local.env` file is git-ignored.

# MODEL_NAME=Qwen/Qwen3-14B-AWQ
# MODEL_GPU_MEMORY_UTILIZATION=0.75
# MODEL_MAX_MODEL_LEN=4096
# MODEL_MAX_NUM_SEQS=1
# MODEL_SWAP_SPACE=8
# MODEL_EXTRA_ARGS=--disable-log-requests
